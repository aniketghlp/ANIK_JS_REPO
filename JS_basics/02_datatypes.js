"use strict"; // treat all JS code as newer version

// alert("hello") // cannot use alert in node JS , it can be used in browser only
let name = "Aniket" //string data type
let age = 32 // number dt  range 2^53 // if number is big use bigint
let isLoggedin = false // boolean dt
// null dt standalone value ==> empty value // object typeof
// undefined  ==> undefined typeof

let state = null

// symbol ==> unique 

// object 

console.log(typeof state);
